# Generated Retrieval Data

This directory is used for local retrieval artifacts created by the ingestion scripts.

Ignored generated paths:

- `data/generated/`
- `data/chroma/`
- `data/tmp/`
- `data/scraped/`

Run `./ingest.sh` after placing approved Kelley PDFs in `knowledge/`.
